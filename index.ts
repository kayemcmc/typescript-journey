//Variable types

//EXPLICIT TYPES

//lets assign example one to a boolean
let example1: boolean = true;

let example2: number = 35;

let example2a: number;

let example3: string = 'Hello world';

//multiple variables but single types are better
let example1a: boolean || number = true;

//subtypes if you dont know what the variable will be

let example4: number = undefined;

let example5: undefined = undefined;

let example6: null = undefined;

const arrayExample: number[] = [1, 2, 3, 4];

let example7 = arrayExample.reduce((num1, num2) => num1 + num2);
